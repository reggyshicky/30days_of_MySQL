-- query that returns the ten most populated cities.Order the data
-- by population from the most populated to the least populated city.
SELECT Name, Population FROM city ORDER BY Population DESC LIMIT 10;
